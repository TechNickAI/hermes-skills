# Learned senders

Stable sender patterns confirmed through repeated observation or explicit user feedback.
This file is evidence, not an autonomous allowlist.

| Sender or domain   | Observed pattern | Recommended default | Evidence                | Confirmed by user? |
| ------------------ | ---------------- | ------------------- | ----------------------- | ------------------ |
| `<sender.example>` | `<pattern>`      | keep                | `<dates or correction>` | no                 |

## Promotion rule

A model classification does not become a permanent rule by itself. Promote a pattern to
`rules.md` only after repeated consistent observations or explicit user confirmation.
